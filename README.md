# S3-Chatbot
A single-page, interactive quiz chatbot interface. The project demonstrates how to connect a lightweight web frontend directly to conversational AI backend services and host it in the cloud.
